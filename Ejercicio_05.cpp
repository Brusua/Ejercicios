#include <stdio.h>
#include <stdlib.h>
int main (void){
	int edad;
	float total;
	float precio=75;
	printf("Edad del clinete\n");
	scanf("%d",&edad);
if (edad<7){
	(total=precio*0.4);
}else if (edad>60)
	(total=precio*0.45);
else if
	(total=precio);

printf("El precio del boleto es:%f \n", total);
}
