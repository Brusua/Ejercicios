#include <stdio.h>
#include <stdlib.h>
int main (void){
	float f,x;
	printf("introduzca el valor de x");
	scanf("%f",&x);
if (x<=0){
	(f=x+3);
	printf("El resultado de la ecuacion es:%f",f);
}
else if (x>0)
	(f=(x*x)+2*x);
	printf("El resultado de la escuacion es:%f",f);
	}
